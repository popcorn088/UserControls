<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="UserControls.NumericUpDown.NumericUpDown"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UserControls.NumericUpDown"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid DataContext="{Binding ElementName=root}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" Text="{x:Bind Convert(Value), BindBack=ConvertBack, Mode=TwoWay}"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <RepeatButton Grid.Row="0" Command="{x:Bind UpCommand}"/>
            <RepeatButton Grid.Row="1" Command="{x:Bind DownCommand}"/>
        </Grid>
    </Grid>
</UserControl>
